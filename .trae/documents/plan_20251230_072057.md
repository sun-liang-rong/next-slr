## 分析结果

### 一、未使用的依赖

#### 1. `markdown-to-jsx` (v9.5.0)
- 在 `app\home\articles\[id]\page.tsx` 中导入但未实际使用
- 实际使用 `react-markdown` 渲染内容

#### 2. `@bytemd/plugin-highlight` (v1.22.0)
- 在 `app\admin\home\articles\components\markdown-editor.tsx` 中配置但未生效
- 代码高亮功能已由 `highlight.js` 替代实现

### 二、未使用的代码

#### 1. **未使用的 import**
- `app\admin\home\articles\components\article-form.tsx`:
  - `ContentRenderer` 组件被导入但从未使用
  ```typescript
  import ContentRenderer from "@/components/MarkdownRenderer" // 未使用
  ```

- `app\admin\home\articles\[id]\page.tsx`:
  - 第22行 `console.log('Fetched article data:', data.data)` 为调试代码

#### 2. **重复功能的组件**
- 项目中存在两个 Markdown 渲染组件，实际只用到一个：
  - `markdown-to-jsx` (未使用)
  - `react-markdown` + `remark-gfm` (实际使用)

#### 3. **未使用的文件**
- `scripts\seed-user.js` - 种子脚本

### 三、清理计划

1. **删除 `package.json` 中的依赖**：
   - `markdown-to-jsx`
   - `@bytemd/plugin-highlight`

2. **删除未使用的 import**：
   - `article-form.tsx` 中的 `ContentRenderer` 导入
   - `app\home\articles\[id]\page.tsx` 中的 `Markdown` 导入

3. **删除调试代码**：
   - `app\admin\home\articles\[id]\page.tsx` 第22行的 console.log