1. **修改富文本编辑器布局**：将 `article-form.tsx` 中的单一富文本编辑器替换为左右分栏布局
2. **添加实时预览功能**：

   * 左侧：保留现有的 TinyMCE 富文本编辑器

   * 右侧：使用现有的 `ContentRenderer` 组件实现实时预览
3. **调整样式**：确保左右分栏布局美观，响应式设计
4. **优化用户体验**：

   * 实时同步编辑内容到预览区域

   * 保持编辑和预览区域的高度一致

   * 确保滚动同步

**修改文件**：

* `app/admin/home/articles/components/article-form.tsx`：修改表单布局，添加预览区域

* `app/admin/home/articles/components/rich-text-editor.tsx`：（可选）优化编辑器配置

**技术方案**：

* 使用 CSS Grid 或 Flexbox 实现左右分栏

* 利用 React 的状态管理实现编辑内容的实时同步

* 复用现有的 `ContentRenderer` 组件进行 HTML 内容预览

